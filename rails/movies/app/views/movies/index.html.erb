


<div class="panel ">
<div class="row">
	<div class="medium-4 columns">
			
	</div>
	<div class="medium-8 columns">
		
	<h1>Movies</h1>
	</div>
</div>
</div>







 <p> Es sind  <%= @movies.count == 1 ?  "ist " : "sind" %> <%= "#{pluralize(@movies.count, "Movie")}" %> vorhanden </p>
<p><%= link_to "Neuen Film erstellen", new_movie_path %></p>

<table border= 1pt width= 100%>
 		<tr>
 			<th>Titel</th>
 			<th>Rank</th>
 			<th>Umsatz</th>
 			<th>Kinostart</th>
 			<th>Beschreibung</th>
 			<th>Anzeigen</th>
 			<th>Bearbeiten</th>
 			<th>Löschen</th>


 		</tr>

 		<% @movies.each do |movie| %>
 		<tr>
 			<td><%= movie.title %></td>
 			<td><%= movie.rating %></td>
 			<td><%= format_total_gross(movie) %></th>
 			<td><%= time_ago_in_words(movie.kinostart) %></td>
 			<td><%= truncate(movie.description, lenght:40) %></td>

 			<td> <%= link_to "Show", movie_path(movie.id)%> </td>
 			<td> <%= link_to "Edit", edit_movie_path(movie.id)%> </td>
 			<td> <%= link_to "delete", movie_path(movie.id), method: :delete, data: {confirm: "Sicher???"} %></td>

 		</tr>
 		<% end %>
 		
 	</table>